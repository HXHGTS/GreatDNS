## Debian安装源-清华大学

### Debian 8

```
rm -f /etc/apt/*.list
curl -sSL https://raw.githubusercontent.com/HXHGTS/GreatDNS/master/source/debian/tsinghua/debian10_%20tsinghua_sources.list > /etc/apt/sources.list && apt update
```

### Debian 9

```
rm -f /etc/apt/*.list
curl -sSL https://raw.githubusercontent.com/HXHGTS/GreatDNS/master/source/debian/tsinghua/debian10_%20tsinghua_sources.list > /etc/apt/sources.list && apt update
```

### Debian 10

```
rm -f /etc/apt/*.list
curl -sSL https://raw.githubusercontent.com/HXHGTS/GreatDNS/master/source/debian/tsinghua/debian10_%20tsinghua_sources.list > /etc/apt/sources.list && apt update
```

### Debian 11

```
rm -f /etc/apt/*.list
curl -sSL https://raw.githubusercontent.com/HXHGTS/GreatDNS/master/source/debian/tsinghua/debian11_%20tsinghua_sources.list > /etc/apt/sources.list && apt update
```
